<%= form_for(@financer) do |f| %>
  <% if @financer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@financer.errors.count, "error") %> prohibited this financer from being saved:</h2>

      <ul>
      <% @financer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
